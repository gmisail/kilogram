extern print: (string) -> int
extern int_to_string: (int) -> string

enum List('T)
	Cons('T, List),
	Nil
end

let valid: List(int) = Cons(1, Cons(2, Cons(3, Nil)))
let invalid: List(int) = Cons(1, Cons(false, Cons("Hello!", Nil)))

0
