record Album
	name: string,
	artist: string,
	year: int
end

record FakeAlbum
	mock: string
end

let bar: int = 100
let foo: float = 10.3 
let buz: bool = true
let baz: string = "wahoo!"
let taboo: Album = Album {
	name: "TA300",
	artist: "Denzel Curry",
	year: 2018
}

let get_service: (int) -> string = function(port: int): string 
	if port == 22 then "ssh" else "unknown"	
end

bar
