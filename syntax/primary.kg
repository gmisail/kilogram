record Album
	name: string,
	artist: string
end

record FakeAlbum
	mock: string
end

let bar: int = 100
let foo: float = 10.3 
let buz: bool = true
let baz: string = "wahoo!"
let taboo: Album = Album {
	name: "TA300",
	artist: "Denzel Curry"
}

let is_odd: bool = if false then true else false 

let demo: (int) -> bool = function(x: int): bool 
	"he" == "llo"
end

let get_localhost: (int) -> string = function(port: int): string 
	"localhost:8888"  
end

bar
