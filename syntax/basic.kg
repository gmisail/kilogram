enum Sequence 
	Cons(int, Sequence),
	Nil
end

let rec length = function(seq: Sequence): int 
	case seq of
		Cons(num, tail) -> 1 + length(tail),
		Nil -> 0
	end
end

length(Cons(1, Cons(2, Cons(3, Nil))))
