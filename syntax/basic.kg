extern int_to_string: (int) -> string
extern print: (string) -> int

enum List 
	None,
	Cons(int, List)
end

let test: List = None

let rec make_list = function(size: int): List
	if size > 0 then 
		Cons(size, make_list(size - 1))
	else 
		None
end

0
