record Token 
	value: string
end

let show_message: (string) -> string =
function(message: string): string
	"Hello world!"
end

let increment_by: (int) -> (int) -> int =
function(x: int): (int) -> int
	function(y: int): int
		x + y
	end
end

let five_inc: (int) -> int = increment_by(5)

five_inc(10)
