extern int_to_string: (int) -> string
extern print: (string) -> int

enum Maybe
	None,
	Some(int)
end

let fetch = function(url: string): Maybe 
	if url == "http://gmisail.me" then
		Some(10)
	else
		None
end

fetch("http://gmisail.me")
